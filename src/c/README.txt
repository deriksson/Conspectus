This directory contains the source code for a UDP client for sending queries to a Conspectus server. 
The client code is written in ANSI C and might be used instead of the Java client for ligthening fast 
execution. The client program should typically be installed in the /usr/local/bin directory.

##Installation

  make
  sudo make install

##Usage
 
  query <hostname> <port> <query>

