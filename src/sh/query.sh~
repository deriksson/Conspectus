#!/bin/bash
# Looks up synonyms in local text file.
#
# Usage:
# the <search term>
#
HERE="$(dirname "${BASH_SOURCE[0]}")"
FILE=$HERE/../../data/sylex-2013-12-28.txt
SEARCH_TERM="$1"
#
grep "^"$SEARCH_TERM": " $FILE | sed "s/^[a-zA-Z ]*: //g"
